Classify the following user input into one of these categories:
- CONTENT: The user is providing document text, academic content, or information to be analyzed and stored
- INTENT: The user is asking a question, requesting an action, or issuing a command (e.g., "find papers about X", "what is Y?")
- GREETING: The user is greeting, thanking, or making small talk (e.g., "hello", "thanks")
- MIXED: The user input contains both content AND intent/greeting in one message

Respond with ONLY the label name and a confidence 0-100 on one line each, followed by optional reasoning.
Format:
LABEL: [CONTENT|INTENT|GREETING|MIXED]
CONFIDENCE: [0-100]
CONTENT_PORTION: [if MIXED, the content part]
INTENT_PORTION: [if MIXED, the intent part]

User input:
"{text}"

Response: